name: Caller Workflow

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  call-reusable:
    uses: ruskinb1/test-passing-secret/.github/workflows/reusable.yml@main
    with:
      bool_input: "true"
      another_bool: 'true'
